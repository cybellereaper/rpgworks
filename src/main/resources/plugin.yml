main: ${PACKAGE}.${NAME}
name: ${NAME}
version: "${VERSION}"
api-version: "1.21.11"
author: AUTHOR
description: DESCRIPTION
commands:
  gear:
    description: Generate and grant RPG gear
    usage: "/gear <player> <template> <rarity> <level>"
    permission: rpgworks.command.gear
permissions:
  rpgworks.command.gear:
    description: Allows using /gear to generate items
    default: op
